var string k, int l;

k = 'test';
if (k == 'test') {
	k = 'A';
} else {
	l = 2;
}

boolean function isObject(int x, string h, int rrr)
{
   var int m, ob r;
   m = 0;
   x = 8;
   return obkind(x) > 15;
}

